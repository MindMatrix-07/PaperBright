# Tailwind v4 Skill Notes

Use Tailwind v4 through the Vite plugin:

- `@import "tailwindcss";` in `src/styles/global.css`.
- Configure theme tokens in CSS with `@theme`.
- Avoid viewport-scaled text and unstable toolbar dimensions.
- Use neutral surface, border, and muted text tokens before adding new colors.
